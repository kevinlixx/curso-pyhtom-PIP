import charts
def run():
    charts.generate_pie_chart()

if __name__ == '__main__': #si el archivo es ejecutado directamente desde la terminal o consola de comandos se ejecutará el código de la función run
    run()

